// @ts-check

/**
 * Initializes the project
 * @param {object} config 
 * @param {boolean} config.debug
 * @param {string} config.url
 * @returns {void}
 */
function init(config) {
    return;
}

/**
 * Exits the program
 * @param {number} code 
 * @returns {number}
 */

function exit(code) {
    return code + 1;
}